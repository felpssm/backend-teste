version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./prisma:/app/prisma
      - ./src:/app/src
    environment:
      NODE_ENV: development
    command: sh -c "npx prisma migrate deploy && npm run start:dev"
